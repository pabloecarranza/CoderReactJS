import React from 'react';

const Contacto = ({greeting}) => {
    document.title = greeting
    return (
        <div>
            <h1>CONTACTO</h1>
        </div>
    );
}

export default Contacto;
